dist: trusty
sudo: required

addons:
  sonarcloud:
    organization: "felberto-github"
    token:
      secure: ${SONAR_TOKEN}

language: node_js
node_js:
  - "stable"
cache:
  directories:
    - "node_modules"
install:
  - npm install

env:
  global:
    - NODE_ENV=test

script:
  - npm run test
  - npm run build
  - sonar-scanner

after_success:
  - npm run coverage

deploy:
  provider: heroku
  app: uno-client
  api_key:
    secure: ${HEROKU_API_KEY}
  on:
    branch: release